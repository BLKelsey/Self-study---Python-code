# Please write a function named range_of_list, which takes a list of integers as an argument. The function returns
# the smallest and the largest value in the list.

range_of_list = [12, 4, 35, 9, 62]

greatest = max(range_of_list)
smallest = min(range_of_list)
print('The range of the list is',smallest, greatest)